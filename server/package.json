{
  "name": "turnos-server",
  "private": true,
  "type": "module",
  "description": "API de sincronizacion a Postgres + chatbot de consulta (OpenAI + MCP)",
  "scripts": {
    "start": "node server.mjs",
    "chat": "node chat.mjs",
    "selftest": "node chat.mjs --selftest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "pg": "^8.13.0"
  }
}
